# Deprecated DeepSeek-V4-Pro 1.6T Single-turn 8k1k entries archived from
# amd-master.yaml. Deprecated after Tuesday, September 8, 2026 (see the
# Deprecation Notice in MODELS.md). Agentic coding is unaffected and stays
# active for dsv4, including its MTP and DSpark arms; the model is not retired.
# Removed from the active master config so sweep generation no longer selects them.

# DeepSeek-V4-Pro FP8 single-node on MI300X (gfx942) via vLLM.
# EXTRAPOLATED bring-up. sglang was abandoned: on gfx942 (no native FP4) the
# dsv4 sglang backend's nvfp4 MoE / TileLang-MLA kernels have no gfx942 build
# (gfx950/MI355X only). vLLM runs the checkpoint in FP8 via --quantization
# deepseek_v4_fp8 (dequant FP4 MoE -> FP8), the same path the H200 dsv4 vLLM
# recipe uses. Config mirrors the same-model dsv4-fp4-mi355x-vllm (TP8, conc
# 4-512); the FP4->FP8 dequant (~1.05TB) fits 8x192GB only at TP8. Launch
# script dsv4_fp8_mi300x.sh carries the deepseek_v4 + gfx942 AITER flags.
dsv4-fp8-mi300x-vllm:
  image: vllm/vllm-openai-rocm:nightly-09663abde0f50944a8d5ea30120666024b503faa
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi300x
  precision: fp8
  framework: vllm
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # 8k1k KV fits only ~20x concurrency (9472-token requests), so conc512 is
      # ~25x oversubscribed -> request timeouts. conc256 is proven green; cap here.
      - { tp: 8, conc-start: 4, conc-end: 256 }

# MTP variant of dsv4-fp8-mi300x-vllm. Mirrors the base recipe and adds
# DeepSeek-V4 built-in MTP via --speculative-config (num_speculative_tokens=2),
# routing to dsv4_fp8_mi300x_mtp.sh; benchmark uses --dsv4 chat-template
# encoding (required for meaningful MTP acceptance).
dsv4-fp8-mi300x-vllm-mtp:
  image: vllm/vllm-openai-rocm:nightly-09663abde0f50944a8d5ea30120666024b503faa
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi300x
  precision: fp8
  framework: vllm
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # 8k1k MTP KV is tighter than normal (draft model): conc256 failed here
      # (19.2% req failures) and conc512 was 55.8%; conc128 passed cleanly.
      # Cap 8k1k MTP at 128 (normal holds 256, 1k1k holds 512).
      - { tp: 8, conc-start: 4, conc-end: 128, spec-decoding: mtp }

dsv4-fp4-mi355x-sglang-disagg:
  image: lmsysorg/sglang-rocm:v0.5.14-rocm720-mi35x-20260701
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: cluster:mi355x-amds
  precision: fp4
  framework: sglang-disagg
  router: { name: sglang-router, version: "0.3.2" }
  kv-p2p-transfer: mori
  multinode: true
  disagg: true
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # non-MTP configurations
      # 1P1D pure TP8  (mori KV transfer)
      - spec-decoding: "none"
        conc-list: [ 1, 2, 4, 8, 16, 32, 64, 128 ]
        prefill:
          num-worker: 1
          tp: 8
          ep: 1
          dp-attn: false
          additional-settings:
          - "PREFILL_NODES=1"
        decode:
          num-worker: 1
          tp: 8
          ep: 1
          dp-attn: false
          additional-settings:
          - "DECODE_NODES=1"
          - "DECODE_MTP_SIZE=0"
      # 1P1D DEP8  (mori KV transfer + mori MoE a2a, dp-attention)
      - spec-decoding: "none"
        conc-list: [ 256 ]
        prefill:
          num-worker: 1
          tp: 8
          ep: 8
          dp-attn: true
          additional-settings:
          - "PREFILL_NODES=1"
        decode:
          num-worker: 1
          tp: 8
          ep: 8
          dp-attn: true
          additional-settings:
          - "DECODE_NODES=1"
          - "DECODE_MTP_SIZE=0"

      - spec-decoding: "none"
        conc-list: [ 512 ]
        prefill:
          num-worker: 1
          tp: 8
          ep: 8
          dp-attn: true
          additional-settings:
          - "PREFILL_NODES=1"
        decode:
          num-worker: 1
          tp: 8
          ep: 8
          dp-attn: true
          additional-settings:
          - "DECODE_NODES=1"
          - "DECODE_MTP_SIZE=0"

# MTP variant of dsv4-fp4-mi355x-sglang-disagg (base run 29821778206). Mirrors
# the base disagg search space and topologies exactly (same 20260701 image,
# mori KV transfer + MoE a2a, sglang-router --pd-disaggregation) and enables
# DeepSeek-V4 built-in MTP on the decode side via spec-decoding: mtp — the same
# enablement path as dsr1-fp8-mi355x-sglang-disagg-mtp. DECODE_MTP_SIZE=1 (draft
# depth 1) on every arm. Depth 2 was tried on the pure-TP8 (bottom-of-curve) arm,
# but this 20260701 build's 2-step EAGLE verification is lossy: greedy gsm8k fell
# to ~0.85 (below the 0.91 dsv4 floor), while depth 1 is lossless (~0.96, matching
# the spec-none base). So all arms use depth 1. The dsv4 ROCm MTP
# CUDA-graph fix (sgl-project/sglang#26383, merged 2026-05-27) is already
# present in the pinned 20260701 nightly, so no image bump is needed.
dsv4-fp4-mi355x-sglang-disagg-mtp:
  image: lmsysorg/sglang-rocm:v0.5.15-rocm720-mi35x-20260713
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: cluster:mi355x-amds
  precision: fp4
  framework: sglang-disagg
  router: { name: sglang-router, version: "0.3.2" }
  kv-p2p-transfer: mori
  multinode: true
  disagg: true
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # MTP configurations
      # 1P1D pure TP8  (mori KV transfer)
      - spec-decoding: "mtp"
        conc-list: [ 1, 2, 4, 8, 16, 32, 64, 128 ]
        prefill:
          num-worker: 1
          tp: 8
          ep: 1
          dp-attn: false
          additional-settings:
          - "PREFILL_NODES=1"
        decode:
          num-worker: 1
          tp: 8
          ep: 1
          dp-attn: false
          additional-settings:
          - "DECODE_NODES=1"
          - "DECODE_MTP_SIZE=2"
      # 1P1D DEP8  (mori KV transfer + mori MoE a2a, dp-attention)
      - spec-decoding: "mtp"
        conc-list: [ 256 ]
        prefill:
          num-worker: 1
          tp: 8
          ep: 8
          dp-attn: true
          additional-settings:
          - "PREFILL_NODES=1"
        decode:
          num-worker: 1
          tp: 8
          ep: 8
          dp-attn: true
          additional-settings:
          - "DECODE_NODES=1"
          - "DECODE_MTP_SIZE=1"

      - spec-decoding: "mtp"
        conc-list: [ 512 ]
        prefill:
          num-worker: 1
          tp: 8
          ep: 8
          dp-attn: true
          additional-settings:
          - "PREFILL_NODES=1"
        decode:
          num-worker: 1
          tp: 8
          ep: 8
          dp-attn: true
          additional-settings:
          - "DECODE_NODES=1"
          - "DECODE_MTP_SIZE=1"

dsv4-fp4-mi355x-sglang:
  image: lmsysorg/sglang-rocm:v0.5.14-rocm720-mi35x-20260706
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi355x
  precision: fp4
  framework: sglang
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      - { tp: 8, dp-attn: true, conc-start: 64, conc-end: 2048 }
      - { tp: 4, dp-attn: true, conc-start: 16, conc-end: 128 }
      - { tp: 4, dp-attn: false, conc-start: 1, conc-end: 32 }

# MTP variant of dsv4-fp4-mi355x-sglang. Mirrors the base search space and adds
# spec-decoding: mtp, which routes to dsv4_fp4_mi355x_sglang_mtp.sh (EAGLE
# speculative decoding), per sgl-project/sglang#26383 ([AMD][DSV4] DSV4 MTP
# graph + sparse triton attn optimizations, merged to main 2026-05-27). That PR
# fixes the ROCm HIP-radix MTP CUDA-graph bug (the false-EOS symptom in sgl
# #20404) and validates GSM8K 0.950 with MTP on.
dsv4-fp4-mi355x-sglang-mtp:
  image: lmsysorg/sglang-rocm:v0.5.19-rocm720-mi35x-20260907
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi355x
  precision: fp4
  framework: sglang
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      - { tp: 8, dp-attn: true, conc-start: 64, conc-end: 2048, spec-decoding: mtp }
      - { tp: 8, dp-attn: false, conc-list: [1, 2, 4, 16, 32], spec-decoding: mtp }

# DSv4 on MI355X via vLLM, using the official vllm/vllm-openai-rocm
# nightly image. DSv4 base ROCm support (vllm-project/vllm#40871) merged
# on 2026-05-05, so any nightly built after that includes the
# DeepseekV4ForCausalLM model class.
#
# IMPORTANT: pin to a digest-suffixed nightly tag rather than the
# floating `:nightly`. launch_mi355x-amds.sh caches enroot squashfs
# files keyed on the image string and short-circuits re-import if the
# file already exists, so the floating tag silently keeps a stale build
# even after Docker Hub updates `:nightly`.
#
# DeepSeek-V4-Pro is FP4+FP8 mixed (FP4 MoE expert weights, FP8 for the
# rest); InferenceX classifies this as fp4 — same as the sister sglang
# and atom DSv4 mi355x entries below. The serving recipe enables AITER,
# shared-expert fusion, and INT4 quick-reduce explicitly; the pinned nightly
# then auto-selects its ROCm DeepSeek-V4 mHC, sparse-indexer, attention, and
# compressor-GEMM optimizations. Shared-expert fusion (FHMoE) needs
# vllm-project/vllm#53161, first shipped in this nightly; on earlier builds
# it self-disabled because this checkpoint path was not eligible.
dsv4-fp4-mi355x-vllm:
  image: vllm/vllm-openai-rocm:nightly-d9105ea8001e0a6d77a96327d17515bb5791fb36
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi355x
  precision: fp4
  framework: vllm
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      - { tp: 8, conc-start: 4, conc-end: 512 }

# MTP variant of dsv4-fp4-mi355x-vllm. Mirrors the base recipe's search space
# and adds spec-decoding: mtp, which routes to dsv4_fp4_mi355x_vllm_mtp.sh
# (--speculative-config '{"method":"mtp","num_speculative_tokens":2}'), per
# vllm-project/vllm#43385 (ROCm DeepSeek-V4 MTP, merged 2026-05-24, included in
# v0.22.0). Full conc 4-512 range maps the complete crossover curve: MTP wins
# at low batch (PR perf data: +75% @ conc1, +38% @ conc8) and falls behind STP
# above ~conc32 (-37% @ conc32). Image reuses the base entry's pinned ROCm
# nightly, which includes the MTP path and subsequent DSV4 kernel updates.
dsv4-fp4-mi355x-vllm-mtp:
  image: vllm/vllm-openai-rocm:nightly-d9105ea8001e0a6d77a96327d17515bb5791fb36
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi355x
  precision: fp4
  framework: vllm
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      - { tp: 8, conc-start: 4, conc-end: 512, spec-decoding: mtp }

dsv4-fp4-mi355x-atom:
  image: rocm/atom-dev:nightly_202607231538
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi355x
  precision: fp4
  framework: atom
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # conc<64: tp4 dp-off (best at low conc); conc>=64: dp-on + TBO.
      # tp8 dp-off low-conc kept as redundancy to recheck the crossover on the new image.
      # dp-on cells always set max_req=conc (recipe .sh) to avoid OOM from the large
      # default max_num_seqs (dp-attn keeps a full KV pool per rank); TBO stays conc>=64.
      - { tp: 4, ep: 1, dp-attn: false, conc-start: 1,  conc-end: 64 }
      - { tp: 4, ep: 1, dp-attn: true,  conc-start: 16, conc-end: 128 }
      - { tp: 8, ep: 1, dp-attn: false, conc-start: 1,  conc-end: 64 }
      - { tp: 8, ep: 1, dp-attn: true,  conc-start: 64, conc-end: 2048 }

dsv4-fp4-mi355x-atom-mtp:
  image: rocm/atom-dev:nightly_202607231538
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi355x
  precision: fp4
  framework: atom
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # Aligns ATOM official DSv4 MTP (models.json): MTP3 dp-off at low-mid conc,
      # DPA MTP3 (dp-attn) at mid-high conc. dp-on starts at c32 (dp-attn helps from c32).
      # prefill-only TBO on dp-attn cells at conc>=256 (measured crossover: TBO hurts
      # c64/c128 -10~14%, helps c256+ +8~14%; MTP removes low-conc latency so TBO there is
      # pure overhead). MTP-safe — only decode-TBO (--enable-tbo all) drops spec metadata.
      - { tp: 8, ep: 1, dp-attn: false, conc-start: 4,  conc-end: 256,  spec-decoding: mtp }
      - { tp: 8, ep: 1, dp-attn: true,  conc-start: 32, conc-end: 1024, spec-decoding: mtp }
      # tp4 removed: tp4+MTP+dp-on OOMs — 806G/4=201G/GPU weight leaves KV budget
      # NEGATIVE (-10.98GB) even at gpu-mem-util 0.90 (run 30277221626). #2327 tp4
      # works only because it's non-MTP (no draft KV layer). MTP stays tp8-only.

# MiniMax-M3 MXFP8 MI355X recipe:
# https://github.com/vllm-project/recipes/commit/2a3728ed9892debfd767a72a58ebc90b33f186e5
# MXFP8 runs from TP=4 on gfx950; block size 128 is mandatory for MSA.
dsv4-fp4-mi355x-atom-disagg:
  image: rocm/atom-dev:nightly_202606101403
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  runner: mi355x
  precision: fp4
  framework: atom-disagg
  router: { name: atomesh, version: "087b82d9c1f630e79149ba37e6213257ec9a76f8" }
  kv-p2p-transfer: mooncake
  multinode: true
  disagg: true
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # 2P1D DPA+TP8 
      - conc-list: [ 256, 512, 768, 1024, 2048 ]
        prefill:
          num-worker: 2
          tp: 8
          ep: 1
          dp-attn: true
          additional-settings:
          - "PREFILL_NODES=2"
        decode:
          num-worker: 1
          tp: 8
          ep: 1
          dp-attn: true
          additional-settings:
          - "DECODE_NODES=1"
      # 1P1D TP8 
      - conc-list: [ 4, 8, 16, 32, 64, 128 ]
        prefill:
          num-worker: 1
          tp: 8
          ep: 1
          dp-attn: false
          additional-settings:
          - "PREFILL_NODES=1"
        decode:
          num-worker: 1
          tp: 8
          ep: 1
          dp-attn: false
          additional-settings:
          - "DECODE_NODES=1"
      # 1P1D TP8

# DeepSeek-V4-Pro FP8 single-node on MI325X (gfx942) via vLLM.
# EXTRAPOLATED bring-up. Same rationale as dsv4-fp8-mi300x-vllm: sglang has no
# gfx942 build of the dsv4 nvfp4 MoE / TileLang-MLA kernels, so vLLM runs the
# checkpoint in FP8 via --quantization deepseek_v4_fp8 (dequant FP4 MoE -> FP8),
# the H200 dsv4 vLLM path. Config mirrors the same-model dsv4-fp4-mi355x-vllm
# (TP8, conc 4-512); 8x256GB (2TB) has ample headroom for the ~1.05TB FP8
# footprint. Launch script dsv4_fp8_mi325x.sh carries the deepseek_v4 + gfx942
# AITER flags.
dsv4-fp8-mi325x-vllm:
  image: vllm/vllm-openai-rocm:nightly-09663abde0f50944a8d5ea30120666024b503faa
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  # mi325x (mi325x-amds pool) runners are down; use the mi325x-tw cluster.
  runner: cluster:mi325x-tw
  precision: fp8
  framework: vllm
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # 8k1k KV fits only ~20x concurrency (9472-token requests), so conc512 is
      # ~25x oversubscribed -> request timeouts. conc256 is proven green; cap here.
      - { tp: 8, conc-start: 4, conc-end: 256 }

# MTP variant of dsv4-fp8-mi325x-vllm. Mirrors the base recipe and adds
# DeepSeek-V4 built-in MTP via --speculative-config (num_speculative_tokens=2),
# routing to dsv4_fp8_mi325x_mtp.sh; benchmark uses --dsv4 chat-template
# encoding (required for meaningful MTP acceptance).
dsv4-fp8-mi325x-vllm-mtp:
  image: vllm/vllm-openai-rocm:nightly-09663abde0f50944a8d5ea30120666024b503faa
  model: deepseek-ai/DeepSeek-V4-Pro
  model-prefix: dsv4
  # mi325x (mi325x-amds pool) runners are down; use the mi325x-tw cluster.
  runner: cluster:mi325x-tw
  precision: fp8
  framework: vllm
  multinode: false
  scenarios:
    fixed-seq-len:
    - isl: 8192
      osl: 1024
      search-space:
      # 8k1k MTP KV is tighter than normal (draft model): conc256 is borderline
      # (MI300X 19.2% req failures, flaked pass->fail across runs) and conc512
      # is 55.8%. conc128 passed cleanly on the memory-tightest SKU (MI300X);
      # cap 8k1k MTP at 128 (normal holds 256, 1k1k holds 512).
      - { tp: 8, conc-start: 4, conc-end: 128, spec-decoding: mtp }
